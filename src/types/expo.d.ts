// Pulls in Expo's ambient types (CSS modules, image requires, process.env.EXPO_OS, ...).
// Lives here instead of the gitignored expo-env.d.ts so it survives cleanups.
/// <reference types="expo/types" />
